@using CvManagementSystem.Entities
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<CvManagementSystem.LocalizationMarker> L
@model CvManagementSystem.ViewModels.AttributeFormViewModel
@{
    ViewData["Title"] = L["EditAttribute"];
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow">
            <div class="card-body p-4">
                <h2 class="mb-4">@L["EditAttribute"]</h2>

                <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
                    <input type="hidden" asp-for="RowVersion" />
                    <div asp-validation-summary="All" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">@L["NameLabel"]</label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">
                            @L["DescriptionOptional"]
                        </label>
                        <textarea asp-for="Description"
                                  class="form-control"
                                  rows="2"></textarea>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Type" class="form-label">@L["AttributeTypeLabel"]</label>
                        <select asp-for="Type"
                                class="form-select"
                                disabled
                                asp-items="@(Enum.GetValues<AttributeType>().Select(t => new SelectListItem { Value = ((int)t).ToString(), Text = L[$"AttributeType_{t}"] }))">
                        </select>
                        <input type="hidden" asp-for="Type" />
                        <small class="text-muted">
                            @L["TypeChangeWarning"]
                        </small>
                    </div>

                    @if (Model.Type == AttributeType.Dropdown)
                    {
                        <div class="card bg-light mb-3">
                            <div class="card-body">
                                <h5 class="card-title">@L["DropdownOptions"]</h5>

                                <div id="optionsList">
                                    @for (int i = 0; i < Model.Options.Count; i++)
                                    {
                                        <div class="input-group mb-2 option-item">
                                            <input type="text"
                                                   name="Options[@i]"
                                                   value="@Model.Options[i]"
                                                   class="form-control" />
                                            <button type="button"
                                                    class="btn btn-outline-danger remove-option-btn">
                                                ✕
                                            </button>
                                        </div>
                                    }
                                </div>

                                <div class="input-group mt-2">
                                    <input type="text"
                                           id="newOptionInput"
                                           class="form-control"
                                           placeholder="@L["NewOptionPlaceholder"]" />
                                    <button type="button"
                                            id="addOptionBtn"
                                            class="btn btn-outline-primary">
                                        @L["AddButton"]
                                    </button>
                                </div>
                            </div>
                        </div>
                    }

                    <div class="mb-3">
    <label asp-for="Category" class="form-label">@L["CategoryLabel"]</label>
    <select asp-for="Category" class="form-select"
            asp-items="@(Enum.GetValues<AttributeCategory>().Select(c => new SelectListItem { Value = ((int)c).ToString(), Text = L[$"AttributeCategory_{c}"] }))">
    </select>
</div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">@L["SaveButton"]</button>
                        <a asp-action="Index" class="btn btn-outline-secondary">@L["CancelButton"]</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    @if (Model.Type == AttributeType.Dropdown)
    {
        <script>
            var optionCount = @Model.Options.Count;

            function addOption() {
                var input = document.getElementById('newOptionInput');
                var value = input.value.trim();

                if (value === '') return;

                var optionsList = document.getElementById('optionsList');

                var div = document.createElement('div');
                div.className = 'input-group mb-2 option-item';
                div.innerHTML =
                    '<input type="text" ' +
                    'name="Options[' + optionCount + ']" ' +
                    'value="' + value + '" ' +
                    'class="form-control" />' +
                    '<button type="button" ' +
                    'class="btn btn-outline-danger remove-option-btn">✕</button>';

                optionsList.appendChild(div);
                optionCount++;
                input.value = '';
                input.focus();
            }

            document.getElementById('addOptionBtn')
                .addEventListener('click', function() {
                    addOption();
                });

            document.getElementById('newOptionInput')
                .addEventListener('keypress', function(e) {
                    if (e.key === 'Enter') {
                        e.preventDefault();
                        addOption();
                    }
                });

            document.getElementById('optionsList')
                .addEventListener('click', function(e) {
                    if (e.target.classList.contains('remove-option-btn')) {
                        e.target.closest('.option-item').remove();
                    }
                });
        </script>
    }
}
